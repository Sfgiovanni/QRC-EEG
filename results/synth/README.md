# Synthetic Gate 2 results

`theory_predictions_frozen.csv` was hashed before nonlinear measurement. The mechanical verdict
in `gate2_report.md` is `SUPPORTED` and remains unchanged. `gate2_postgate_sensitivity.csv` is an
analytical-only diagnostic separating between-process scale from within-process ordering; it does
not rerun reservoirs or modify the frozen protocol.
