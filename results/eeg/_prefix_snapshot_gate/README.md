# Frozen gate-input snapshot

This directory preserves the official r=0.7 EEG rows used to reproduce available Gate 3 exact
baselines after the working tree later diverged. It is a provenance/reference snapshot; final
scientific claims must use the indexed canonical Gate outputs and explicitly documented baseline
scope.
