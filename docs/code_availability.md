# Code availability preparation

The analysis code is organized under `src/qrc_eeg/` and `scripts/`, with frozen configuration in
`config/`. The current local state is not a published release and contains uncommitted work.
`LICENSE` applies to code; third-party data retain their own terms.

Human actions still required: review authorship, create a clean tagged commit, choose an archive,
and insert an actual persistent identifier only after deposition. This stage performs none of
those actions.
