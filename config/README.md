# Configuration index

- `eeg_frozen.yaml`: corrected EEG splits, seeds, horizons and grids.
- `effective_kernel_gate1_frozen.json`: local mechanism check.
- `rotaA_stage2_frozen.json`: synthetic validation.
- `rotaA_gate3_frozen.json`: resource and finite-shot characterization.

Execution scripts validate committed HP/configuration hashes. Never silently substitute the
exploratory r=0.9 state for the official r=0.7 Gate 1 configuration.
